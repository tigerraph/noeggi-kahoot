{
  "name": "noeggi-kahoot",
  "private": true,
  "scripts": {
    "build": "python3 scripts/build.py",
    "verify": "node scripts/verify.js",
    "verify:online": "node scripts/verify-online.js",
    "status": "node scripts/status.js",
    "deploy": "npm run build && npm run verify && cp dist/index.html index.html && git add -A && git commit -m \"deploy: $(grep -o 'Build [0-9.]* · [0-9:]*' index.html)\" && git push"
  },
  "devDependencies": {
    "@fontsource/archivo-black": "^5.0.0",
    "@fontsource/nunito": "^5.0.0",
    "@fontsource/press-start-2p": "^5.0.0",
    "puppeteer": "^23.0.0"
  }
}
